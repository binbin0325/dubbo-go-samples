# dubbo client yaml configure file

dubbo:
  registries:
    demoNacos:
      protocol: nacos
      address: 127.0.0.1:8848
  consumer:
    references:
      UserProvider:
        protocol: dubbo
        interface: org.apache.dubbo.UserProvider
  logger:
    zap-config:
      level: info